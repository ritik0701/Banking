import { Changetranspassword } from './changetranspassword';

describe('Changetranspassword', () => {
  it('should create an instance', () => {
    expect(new Changetranspassword()).toBeTruthy();
  });
});
